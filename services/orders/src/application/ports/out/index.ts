/**
 * Application Ports (Output)
 *
 * These are the interfaces that the application layer uses to communicate
 * with the outside world. They are implemented by adapters in the
 * infrastructure layer.
 */
export { EventPublisher } from './EventPublisher';
